(function(a, b) {
    'function' == typeof define && define.amd ? define('dCounts', b) : 'object' == typeof exports && module.exports ? module.exports = b : a.dCounts = b
})(this, function(b, c = 150) {
    if ('string' == typeof b || 'number' == typeof c) {
        const d = document.getElementById(b),
            e = `<div id="counters-${b}" style="display:none;">0 de ${c} caracteres</div>`,
            g = () => d.insertAdjacentHTML('afterend', e),
            h = () => {
                let i = c,
                    j = d.value.length,
                    k = document.getElementById(`counters-${b}`);
                k.innerHTML = `${j} de ${i} caracteres`, j >= i && (d.focus(), d.value = d.value.substring(0, i), k.innerHTML = `Haz alcanzado el límite caracteres`)
                k.style.display = 'block';
            };
        (() => {
            if (null === d) throw new Error(`Something's wrong with your selector`);
            g(), d.addEventListener('keyup', h, !1)
        })()
    }
});